spring:
  application:
    name: authorization
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8081
      fail-fast: true
      username: config
      password: welcome
management:
  security:
    ena